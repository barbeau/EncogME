application.title=EncogDesktopAllCode
application.vendor=Sean
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/EncogModelGenerator.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.encog-core-src=encog-core-1.1.0/encog-core/src
file.reference.encog-core-test=encog-core-1.1.0/encog-core/test
file.reference.examples-src=encog-core-1.1.0/encog-core/examples/src
includes=**
jar.compress=false
javac.classpath=\
    ${libs.Microsoft_SQL_Server_JDBC_Driver.classpath}:\
    ${reference.EncogDesktopAllCode.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=org.encog.examples.neural.xorbackprop.XorBackprop
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.EncogDesktopAllCode=../EncogDesktopAllCode
reference.EncogDesktopAllCode.jar=${project.EncogDesktopAllCode}/dist/EncogDesktopAllCode.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.src.dir=encog-core-1.1.0\\encog-core\\src
src.src2.dir=encog-core-1.1.0\\encog-core\\examples\\src
